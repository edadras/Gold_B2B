<div dir="rtl">

# راهنمای مشارکت

## وضعیت فعلی پروژه

این مخزن در **فاز طراحی** است. هنوز کدی نوشته نشده و همه محتوا
مستندات معماری و طراحی محصول است.

---

## پیش از هر تغییر

### ۱. مستندات مرتبط را بخوانید

</div>

```
حداقل:
  · docs/00-overview/02-glossary.md     زبان مشترک
  · docs/02-architecture/05-adr.md      تصمیمات قبلی
  · سند دامنه مربوط به حوزه کارتان
```

<div dir="rtl">

### ۲. با پنج اصل بنیادین بسنجید

</div>

```
۱. Ledger مبنای حقیقت است — append-only، هرگز UPDATE
۲. مالکیت از محل نگهداری جداست
۳. یک موتور محاسبات واحد برای کل سیستم
۴. معامله ≠ تسویه
۵. هیچ رکورد مالی حذف نمی‌شود
```

<div dir="rtl">

اگر تغییر شما با یکی از این‌ها در تضاد است، **پیش از پیاده‌سازی**
یک ADR بنویسید و درباره‌اش بحث کنید.

---

## تغییر مستندات

### قواعد نوشتن

</div>

```
· زبان: فارسی، با اصطلاحات فنی انگلیسی
· جهت: RTL — استفاده از <div dir="rtl">
· بلوک کد و نمودار: بیرون از div (تا LTR بمانند)
· اعداد در جدول: با جداکننده هزارگان
· هر ادعای فنی: با مثال عددی
· هر تصمیم: با دلیل
· هر ابهام حقوقی: با ⚠️ صریح
```

<div dir="rtl">

### الگوی ساختار فایل

</div>

```markdown
<div dir="rtl">

# عنوان

## بخش

متن فارسی...

</div>

```
بلوک کد یا نمودار ASCII
```

<div dir="rtl">

ادامه متن فارسی...

</div>
```

<div dir="rtl">

### همگام‌سازی اسناد

هر تغییر ممکن است چند سند را متأثر کند:

| تغییر | اسناد وابسته |
|---|---|
| مدل داده | `04-data/01-erd.md`, `04-data/02-schema-mysql.md`, سند دامنه |
| فرمول | `11-appendix/01-formulas.md`, `03-domain/08-calculation-engine.md` |
| وضعیت | `11-appendix/02-state-machines.md`, سند دامنه |
| endpoint | `05-api/02-endpoints.md` |
| رویداد | `02-architecture/03-events.md`, `02-architecture/02-modules.md` |
| اصطلاح | `00-overview/02-glossary.md` |
| تصمیم معماری | `02-architecture/05-adr.md` (ADR جدید، نه ویرایش قبلی) |
| قابلیت | `01-product/03-feature-map.md` |

---

## نوشتن ADR

ADRها **ویرایش نمی‌شوند**. اگر تصمیم قبلی تغییر کرد، ADR جدید بنویسید
و در آن به ADR قبلی ارجاع دهید و وضعیتش را به `Superseded` تغییر دهید.

### قالب

</div>

```markdown
## ADR-0XX — عنوان کوتاه تصمیم

**وضعیت:** پیشنهادی | پذیرفته‌شده | جایگزین‌شده توسط ADR-0YY

**زمینه:** چه مسئله‌ای وجود دارد؟ چه محدودیتی؟

**گزینه‌ها:**
| گزینه | مزیت | عیب |
|---|---|---|

**تصمیم:** چه چیزی انتخاب شد.

**پیامدها:**
- ✅ مزایا
- ⚠️ هزینه‌ها و محدودیت‌ها
- 🔁 مسیر خروج در صورت نیاز

**شرط بازنگری:** چه زمانی باید دوباره بررسی شود؟
```

<div dir="rtl">

---

## قواعد کد (وقتی پیاده‌سازی شروع شود)

### قواعد سخت

</div>

```
۱)  هیچ float/double در مسیر مالی — فقط int و bcmath
۲)  هیچ UPDATE روی ledger_entries و audit_logs
۳)  هیچ فراخوانی HTTP خارجی داخل تراکنش دیتابیس
۴)  هیچ صف/اعلان/broadcast داخل تراکنش
۵)  هیچ عدد جادویی — همه از config یا const
۶)  هر عملیات مالی: تراکنش + قفل + Idempotency + Audit
۷)  قفل‌گذاری همیشه با ترتیب صعودی id
۸)  رویدادها فقط پس از commit منتشر می‌شوند
۹)  ماژول‌ها فقط از طریق Contract یا Event ارتباط می‌گیرند
۱۰) هر گذار وضعیت از ماشین حالت عبور می‌کند
```

<div dir="rtl">

### پیش از ارسال PR

</div>

```bash
composer check     # lint + analyse + deps + test
```

<div dir="rtl">

باید همه سبز باشند:

</div>

```
[ ] pint       — سبک کد
[ ] phpstan    — تحلیل ایستا سطح ۸
[ ] deptrac    — مرزهای ماژول
[ ] pest       — تست‌ها با پوشش ≥ ۸۰٪
[ ] ledger-invariants — ثابت‌های دفتر
[ ] composer audit — آسیب‌پذیری وابستگی‌ها
[ ] gitleaks   — نشت راز
```

<div dir="rtl">

---

## قواعد کامیت

</div>

```
<type>: <خلاصه کوتاه>

<توضیح مفصل — چرا، نه فقط چه چیزی>

types:
  docs     مستندات
  feat     قابلیت جدید
  fix      رفع باگ
  refactor بازآرایی بدون تغییر رفتار
  test     تست
  chore    نگهداری
  perf     بهبود کارایی
  security اصلاح امنیتی
```

<div dir="rtl">

---

## بازبینی کد

### چک‌لیست بازبین

</div>

```
عمومی
[ ] با پنج اصل بنیادین سازگار است
[ ] هیچ float در مسیر مالی
[ ] type-hint کامل
[ ] بدون N+1

مالی
[ ] داخل تراکنش
[ ] قفل با ترتیب ثابت
[ ] Idempotency
[ ] بررسی بقای جرم
[ ] رویداد پس از commit

امنیت
[ ] بررسی tenancy صریح
[ ] بررسی permission
[ ] بدون داده حساس در لاگ/خطا
[ ] Rate limit

تست
[ ] happy path
[ ] مسیرهای خطا
[ ] ثابت‌ها (اگر مالی)
[ ] همزمانی (اگر روی موجودی اثر دارد)

مستندات
[ ] اسناد وابسته به‌روز شده
```

<div dir="rtl">

### تغییراتی که نیاز به تأیید بیشتری دارند

</div>

```
⚠️ نیازمند تأیید معمار:
   · هر تغییر در ماژول Ledger
   · هر تغییر در ماژول Settlement
   · هر تغییر در موتور محاسبات
   · افزودن یا حذف وابستگی بین ماژول‌ها
   · تغییر اسکیمای جدول‌های مالی

⚠️ نیازمند تأیید مسئول انطباق:
   · تغییر در KYC
   · تغییر در قواعد AML
   · تغییر در نگهداری سوابق
   · تغییر در آنچه به اعضا نمایش داده می‌شود

⚠️ نیازمند تأیید حقوقی:
   · هر تغییر در مدل Custody
   · هر تغییر در سازوکار تهاتر
   · هر تغییر در جریمه یا وثیقه
```

<div dir="rtl">

---

## گزارش مسئله

هنگام گزارش مسئله در مستندات یا طراحی، این‌ها را مشخص کنید:

</div>

```
· سند و بخش دقیق
· مسئله چیست (تناقض؟ ابهام؟ خطای عددی؟ فرض نادرست؟)
· اگر پیشنهاد جایگزین دارید، چیست
· آیا با یکی از پنج اصل بنیادین در تضاد است؟
```

<div dir="rtl">

### مسائل با اولویت بالا

</div>

```
🔴 هر تناقض در فرمول‌ها یا قواعد گِردکردن
🔴 هر سناریویی که بقای جرم را نقض کند
🔴 هر race condition شناسایی‌شده
🔴 هر مسیری که رکورد مالی را حذف یا ویرایش کند
🔴 هر نشت داده حساس
🔴 هر تعارض با الزام قانونی
```

</div>
